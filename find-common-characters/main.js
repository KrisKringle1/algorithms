/**
 * @param {string[]} A
 * @return {string[]}
 */
var commonChars = function (array) {
  //loop through array
  //check if a letter exists in all indexes
  //if it does, push to a new array
  //return new array


};
