/**
 * @param {number[][]} grid
 * @return {number}
 */
var islandPerimeter = function (grid) {
  //check each item in the grid
  //if i === 1, perimiter + 4
  //if i > O && grid above is 1, perimiter - 2
  //if j > 0 and grid before is a 1, perimiter - 2
  //return perimiter
};
